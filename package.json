{
  "name": "hello",
  "version": "0.1.0",
  "description": "Hello world",
  "license": "MIT",
  "esy": {
    "build": "dune build -p #{self.name}"
  },
  "scripts": {
    "test": "esy x hello"
  },
  "dependencies": {
    "@opam/dune": "*",
    "@esy-ocaml/reason": ">= 3.4.0 < 3.6.0",
    "ocaml": "~4.8.1000"
  }
}
